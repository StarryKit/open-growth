# Domain Docs

This repo is treated as single-context.

## Read before exploring

- `CONTEXT.md` at the repo root, if present
- `docs/adr/` at the repo root, if present

If the files do not exist yet, proceed silently.

## Layout

Single-context repos use one root `CONTEXT.md` plus `docs/adr/` for ADRs.

## Vocabulary

Use the glossary terms from `CONTEXT.md` when naming concepts in issues, refactors, tests, and ADRs.
